'''Python program that imports the datetime module with
the alias dt and uses the now function to get the current date and
time'''

import datetime as dt
current_time=dt.datetime.now()
print(current_time)